# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build output from src/build/ (Phase 3); never committed.
.build

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.vscode/sftp.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# FTP credentials
.env
sftp.json
sftp-config.json

# Server-side form secrets (upload manually to Hostinger)
public/api/config.local.php

# Wrangler (Cloudflare) local state and local secrets
.wrangler
.dev.vars*

# Claude Code personal permissions (machine-local, not team settings)
.claude/settings.local.json

# Raw analytics exports and local research — not source, not meant for this
# repo long-term. Kept out of the tree rather than deleted; they predate the
# flatten and belong to the owner, not the build.
Google Search Console Downloads/
Claude Prompts/
baseline-2026-09/
SEO_GEO Reports/

# Generated post-build into dist/ by scripts/write-sitemap.mjs (Phase 3.2d).
public/sitemap.xml
