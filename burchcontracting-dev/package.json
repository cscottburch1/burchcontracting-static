{
  "name": "burchcontracting-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-geo-aeo.mjs && node scripts/generate-services.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo Build complete! Now right-click the dist folder and select 'Upload Folder' using SFTP extension"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.12"
  }
}
