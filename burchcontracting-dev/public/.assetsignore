# Files Wrangler must not upload to Cloudflare (.gitignore syntax).
# The PHP backend and Apache config stay on Hostinger; cloudflare/worker.js
# forwards /api/* there. Uploaded as static files, Cloudflare would serve the
# PHP source and database schema as plain text.
.htaccess
api/
