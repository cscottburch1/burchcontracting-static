{
  "name": "burchcontracting-static",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-calculator-tables.mjs && node scripts/generate-trust-layer.mjs && node src/build/index.mjs",
    "build": "vite build && node scripts/apply-staging-noindex.mjs && node scripts/write-sitemap.mjs && node scripts/generate-cloudflare-files.mjs",
    "preview": "vite preview",
    "check-build": "node scripts/check-build.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.12",
    "wrangler": "^4.131.1"
  }
}
